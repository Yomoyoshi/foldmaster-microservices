server:
  port: 8081

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:5432/userdb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  mail:
    from: dmitryayoshin@yandex.ru
    host: smtp.yandex.ru
    port: 465
    username: dmitryayoshin@yandex.ru
    password: ouvftlewvlxzsjdr
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true

jwt:
  secret: mySuperSecretKeyForJWTGeneration123!
  expiration: 86400000  # 24h

logging:
  level:
    com.foldmaster: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG